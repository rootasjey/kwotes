/// An enum representing the displayed text value on settings page.
enum EnumAccountDisplayed {
  /// Display user's email on the settings page.
  email,

  /// Display user's name on the settings page.
  name,
}

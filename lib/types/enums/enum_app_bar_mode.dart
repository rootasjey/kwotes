enum EnumAppBarMode {
  /// App bar is shown on its default state like on home page.
  home,

  /// App bar is shown on its search state.
  /// Some actions are available on this state i.e. search types.
  search,

  /// App bar is shown on its settings state.
  settings,

  /// Hide signin button in this mode.
  signin,
}

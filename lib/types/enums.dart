enum AddQuoteType {
  draft,
  offline,
  tempquote,
}

enum AniProps {
  color,
  height,
  opacity,
  translateX,
  translateY,
  width,
}

enum AppBarDiscover {
  authors,
  references,
  random,
}

enum AppBarQuotesBy {
  authors,
  references,
  topics,
}

enum AppBarSettings {
  allSettings,
  selectLang,
  en,
  fr,
}

enum DiscoverType {
  authors,
  references,
}

enum HeaderViewType {
  options,
  search,
}

enum ItemsLayout {
  list,
  grid,
}

enum ItemComponentType {
  card,
  row,
}

enum QuotePageType {
  favourites,
  list,
  published,
}

enum SnackType {
  error,
  info,
  success,
}

enum ImageShareColor {
  dark,
  light,
  colored,
  gradient,
}

enum ImageShareTextColor {
  auto,
  dark,
  light,
}
